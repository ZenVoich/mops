{
	"type": "module",
	"scripts": {
		"dev": "npm run replica && npm run deploy && npm run declarations && npm run declarations:cli && cd frontend && npm run dev && cd ..",
		"build": "cd frontend && npm run build",
		"deploy": "npm run constants && dfx deploy main",
		"replica": "dfx stop && dfx start --emulator --background --clean",
		"constants": "dfx canister create --all && node constants.js",
		"constants:prod": "dfx canister --network ic create --all && node constants.js prod",
		"declarations": "rsync -avr .dfx/local/canisters/** --exclude='assets/' --exclude='idl/' --exclude='*.wasm' --exclude='*.most' --exclude='*.old.did' frontend/declarations",
		"declarations:cli": "rsync -avr .dfx/local/canisters/** --exclude='assets/' --exclude='idl/' --exclude='*.wasm' --exclude='*.most' --exclude='*.old.did' cli/declarations",
		"build:prod": "NODE_ENV=production npm run constants:prod && dfx build --network ic",
		"deploy:prod": "NODE_ENV=production npm run constants:prod && dfx deploy --network ic"
	},
	"dependencies": {
		"@dfinity/principal": "^0.11.1"
	}
}
