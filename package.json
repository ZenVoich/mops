{
	"type": "module",
	"scripts": {
		"dev": "npm run replica && npm run deploy && npm run decl && npm run decl:cli && cd frontend && npm run dev && cd ..",
		"build": "cd frontend && npm run build",
		"deploy": "npm run const && dfx deploy main && npm run decl && npm run decl:cli",
		"replica": "dfx stop && dfx start --emulator --background --clean",
		"const": "dfx canister create --all && node constants.js",
		"const:prod": "dfx canister --network ic create --all && node constants.js prod",
		"decl": "rsync -avr .dfx/local/canisters/** --exclude='assets/' --exclude='idl/' --exclude='*.wasm' --exclude='*.most' --exclude='*.old.did' frontend/declarations",
		"decl:cli": "rsync -avr .dfx/local/canisters/** --exclude='assets/' --exclude='idl/' --exclude='*.wasm' --exclude='*.most' --exclude='*.old.did' cli/declarations",
		"build:prod": "NODE_ENV=production npm run const:prod && dfx build --network ic",
		"deploy:prod": "NODE_ENV=production npm run const:prod && dfx deploy --network ic"
	},
	"dependencies": {
		"@dfinity/principal": "^0.11.1"
	}
}
